# 1. Pubs routes: Map deep links to the app
/pubs/* /app/index.html 200

# 2. App routes: Map the app container itself
/app/* /app/index.html 200

# 3. Everything else: Route to the root landing page
# We use /index.html explicitly to avoid the ambiguity of just /
/* /index.html 200
